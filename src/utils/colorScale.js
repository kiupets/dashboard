import chroma from 'chroma-js';

export const colorScale = chroma.scale(['#F65D5B', '#FDB845', '#65BD61']).domain([0, 100]);
export const colorRedScale = chroma.scale(['#F65D5B', '#FDB845', '#65BD61']).domain([0, 100]);